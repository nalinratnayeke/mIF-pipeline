# Archived Documentation

These files preserve superseded InstanSeg methods history and WSI cleanup implementation handoffs.
They are retained for historical reference only and do not define current pipeline behavior.

Use the root documentation instead:

- `METHODS.md` for adopted production methodology
- `METHODS_LOG.md` for production development history
- `METHODS_TRAINING.md` for training and model-development history
- `AGENTS.md` for current implementation guardrails and documentation routing

